module align_to_director_task

def Update(self, arg1, arg2):
    local3 = vehicle.GetParticipantCCD(self)
    if local3:
        local4 = vehicle.GetCarCombatForwardDirection(local3)
        local5 = game.GetTransform(self).r2
        local6 = vector.Dot(local4, local5)
        if 0.0 > local6:
            local6 = 0.0
        vehicle.SetAnimCmdAlignForward(self, local4, local6 * local6 * arg1.Property0)
