module char_has_spell_connected

def Exit(self, arg1):
    animation.SetStateBit(self, 59.0, 0.0)

def Update(self, arg1, arg2):
    local3 = animation.GetLinkTarget(self)
    if local3:
        local4 = character.GetLatestSpellInfo(local3)
        if local4.IsCurrent > 0.0:
            if local4.SpellName == game.GameHashString(arg1.SpellName):
                animation.SetStateBit(self, 59.0, 1.0)
