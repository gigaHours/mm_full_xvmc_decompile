module char_rotate_to_camera
import @BAFDB405
import @42D0E955

def Enter(self, arg1):
    RotateToCamera(self)

def Exit(self, arg1):
    RotateToCamera(self)

def RotateToCamera(self):
    local1 = camera.GetCameraTransform()
    local2 = vector.Sub(vector.Set(0.0, 0.0, 0.0, 0.0), local1.r2)
    if 0.999 > math.Abs(local2.y):
        local2.y = 0.0
        local2 = vector.Normalize(local2)
        character.RotateInstantly(self, local2)

def Update(self, arg1, arg2):
    RotateToCamera(self)
