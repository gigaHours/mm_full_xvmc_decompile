module char_player_rotate_to_chum

def Update(self, arg1, arg2):
    local3 = character.GetPlayer()
    if local3:
        local4 = character.GetCharacterByName("chumbucket")
        if local4:
            local5 = character.GetWorldMatrix(local4)
            local6 = character.GetWorldMatrix(local3)
            local7 = vector.Sub(local5.r3, local6.r3)
            local7.y = 0.0
            character.RotateInstantly(local3, local7)
