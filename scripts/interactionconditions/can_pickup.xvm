module can_pickup

def Evaluate(self, arg1):
    local2 = 1.0
    local3 = interaction.GetEntryContextParent(self, local2)
    if local3:
        if game.GetItemPickupable(local3, arg1.delay_time):
            return 1.0
    return 0.0
