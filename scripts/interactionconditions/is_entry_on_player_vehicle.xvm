module is_entry_on_player_vehicle

def Evaluate(self, arg1):
    local2 = arg1.action_identifier
    if self and local2:
        local3 = character.GetVehicle(character.GetPlayer())
        local4 = interaction.GetEntryContextParent(self, local2)
        if local3 and local4 and game.IsObjectEqualTo(local3, local4):
            return 1.0
    return 0.0
