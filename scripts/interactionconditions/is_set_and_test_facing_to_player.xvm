module is_set_and_test_facing_to_player

def Evaluate(self, arg1):
    local2 = interaction.GetCharacter(self)
    local3 = game.GetTransform(character.GetPlayer())
    local4 = game.GetTransform(local2)
    local5 = vector.Sub(local4.r3, local3.r3)
    local5.y = 0.0
    local6 = vector.Normalize(local5)
    local7 = vector.Dot(local4.r2, local6)
    local8 = math.Cos(0.5 * math.DegToRad(arg1.cone_allowed_degrees))
    if local7 > local8:
        return 1.0
    local9 = vector.Scale(local6, -1.0)
    game.SetBlackboardValueToObject(local2, @7949DA76, local6)
    return 0.0
