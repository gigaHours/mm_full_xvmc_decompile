module check_canteen_not_full

def Evaluate(self, arg1):
    local2 = character.GetPlayer()
    local3 = character.GetInventoryItem(local2, "canteen")
    if local3:
        local4 = game.GetEconomyResourceId(local3)
        local5 = game.GetEconomyResourceAmount(local4, true)
        if local5 >= 1.0:
            interaction.SetNamedFailEntry(self, "entry.canteen_full")
            return 0.0
    return 1.0
