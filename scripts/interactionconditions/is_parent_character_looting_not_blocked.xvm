module is_parent_character_looting_not_blocked

def Evaluate(self, arg1):
    local2 = interaction.GetEntryContextParent(self, 18.0)
    if local2:
        if character.IsCharacter(local2):
            local3 = animation.GetStateBit(local2, 30.0)
            if local3 == 1.0:
                return 0.0
            return 1.0
    return 1.0
