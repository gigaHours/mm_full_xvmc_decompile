module veh_mine_collision

def KillCar(self):
    local1 = scriptgo.GetProperties(self)
    local2 = character.GetPlayer()
    if not character.IsInvulnerable(local2):
        local3 = character.GetVehicle(local2)
        if local3:
            local4 = vehicle.GetHealth(local3)
            if local1.kill_percentage >= local4:
                character.InstantDeath(local2)
                vehicle.Destroy(local3)
