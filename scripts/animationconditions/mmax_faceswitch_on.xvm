module mmax_faceswitch_on

def Evaluate(self, arg1):
    local2 = game.GetChild(self, "ScriptInput")
    if local2:
        local3 = scriptgo.GetProperties(local2)
        if local3.face_switch_on:
            return 1.0
    return 0.0
