module char_interaction_veh_is_dead
import @58351C01

def Evaluate(self, arg1):
    local2 = character.GetInteractionUserProxy(self)
    local3 = interaction.GetEntryContextParent(local2, arg1.Property0)
    if local3:
        if vehicle.IsVehicle(local3):
            local4 = lib_entity_proxy.GetBodyHealth(local3)
            if local4:
                if scriptgo.Call(local4, @4CFED023):
                    return 1.0
    return 0.0
