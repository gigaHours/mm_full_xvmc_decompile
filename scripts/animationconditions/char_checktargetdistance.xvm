module char_checktargetdistance

def Evaluate(self, arg1):
    local2 = character.GetWorldMatrix(self)
    local3 = animation.GetLinkTarget(self)
    if local3:
        local4 = character.GetWorldMatrix(local3)
        local5 = vector.Distance(local4.r3, local2.r3)
        if arg1.Property0 >= local5:
            return 1.0
        if local5 > arg1.Property0:
            return 0.0
    return 0.0
